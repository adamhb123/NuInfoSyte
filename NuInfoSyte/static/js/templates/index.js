ANIMATION_LIST = []

function addAnimation(text, mode, color, position){
    ANIMATION_LIST.push({
        "text": text,
        "mode": mode,
        "color": color,
        "position": position
    });
}